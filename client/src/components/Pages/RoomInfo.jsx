import Navbar from '../Navbar'
import RoomImage from '../RoomImage'

const RoomInfo = () => {
    return(
        <div className='w-full'>
            <Navbar/>
            <RoomImage/>
        </div>
    );
}

export default RoomInfo